# Netlify redirects file. /blog is the legacy site's one confirmed-empty stub worth a
# redirect (see T-tools/04-it-processes/hefestus-cannabis-wix-migration-process.md);
# /country and /events-page carried negligible content and negligible risk, so they are
# left to 404 rather than redirected to anything.
/blog /resources/ 301
# /book-a-trial/ was the page's original name before the site's copy moved to demo-based
# language (2026-07-23); redirect it to /book-a-demo/ so any bookmark or inbound link
# from that period still lands on a real page instead of a 404.
/book-a-trial/ /book-a-demo/ 301
